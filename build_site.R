# Compile website pages
#
# IMPORTANT: Run this from the base directory of the project.  You can do this
#            with: "session > set working directory > to source file location"

library(rmarkdown)
rmarkdown::render_site()

